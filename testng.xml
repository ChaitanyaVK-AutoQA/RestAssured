<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="API Test Suite"> <!-- <- name is required -->
    <test name="RestAssured Tests">
        <packages>
            <package name="tests.*" /> <!-- adjust package as per your test classes -->
        </packages>
    </test>
</suite>
